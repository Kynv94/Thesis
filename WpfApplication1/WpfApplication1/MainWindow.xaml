<Window x:Class="WpfApplication1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication1"
        mc:Ignorable="d"
        Title="MainWindow" Height="455.587" Width="798.743">
    <Window.Resources>
        <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        </Style>
    </Window.Resources>
    <Grid>
        <ListView 
         Margin = "10"
         Name = "MenList" IsSynchronizedWithCurrentItem="True" 
         HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">

            <ListView.View>

                <GridView>

                    <GridViewColumn Header = "No." 
                                    DisplayMemberBinding = "{Binding ID}" Width= "70"/>

                    <GridViewColumn Header = "Source" 
                  DisplayMemberBinding = "{Binding Src}" Width="120"/>


                    <GridViewColumn Header = "Destination" 
                  DisplayMemberBinding = "{Binding Des}" Width= "120"/>

                    <GridViewColumn Header = "Protocol" 
                  DisplayMemberBinding = "{Binding Proc}" Width= "70"/>

                    <GridViewColumn Header = "Length" 
                  DisplayMemberBinding = "{Binding Len}" Width= "50"/>

                    <GridViewColumn Header = "Info" 
                  DisplayMemberBinding = "{Binding Info}" Width= "200"/>
                </GridView>

            </ListView.View>

        </ListView>
    </Grid>
</Window>
